# HTTP server
PORT=5000
NODE_ENV=development

# Database (used when database connectivity is added)
MONGODB_URI=mongodb://127.0.0.1:27017/devpilot-ai

# Optional, but recommended to raise GitHub REST API rate limits and analyze private repositories.
GITHUB_TOKEN=
